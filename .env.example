# Copy this file to `.env` and fill in the values.

# SHA-256 hex hash of the administrator password (NOT the password itself).
# Generate: node -e "const c=require('crypto');console.log(c.createHash('sha256').update('YOUR_NEW_PASSWORD','utf8').digest('hex'))"
VITE_ADMIN_PASSWORD_HASH=
